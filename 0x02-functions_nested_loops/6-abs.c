#include "holberton.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: x integer
 * Description: Imprimir el signo de un numero
 * Return: 0 (Success)
*/
int _abs(int n)
{
	if (n < 0)
	{
		n = (-1) * n;
	}
	else 
	{
		n = (1) * n;
	}
	return (0);
}

